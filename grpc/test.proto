syntax = 'proto3';

option go_package = "/grpc";

package test.v1;

service TestService {
  rpc Sum (SumRequest) returns (SumResponse) {}
}

message SumRequest {
  int32 num1 = 1;
  int32 num2 = 2;
}

message SumResponse {
  int32 result = 1;
}